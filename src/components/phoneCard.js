

const PhoneCard = (props) => {
    /* const {id} = useParams(); */
   /*  const [items, setItems] = useState([]);
    const [editField, setEdietField] = useState(false); */
    
return <div>{props.children}</div>
}
  export default PhoneCard;